{
  "name": "@ngois/webhook-dispatcher",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@ngois/config": "*",
    "@ngois/db": "*",
    "@ngois/logging": "*",
    "@ngois/webhook-egress": "*",
    "pg": "^8.16.0",
    "redis": "^4.7.0",
    "zod": "^3.25.28"
  },
  "devDependencies": {
    "@types/pg": "^8.15.2",
    "tsx": "^4.19.4"
  }
}
