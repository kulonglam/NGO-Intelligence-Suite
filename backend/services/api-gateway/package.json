{
  "name": "@ngois/api-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@ngois/config": "*",
    "@ngois/errors": "*",
    "@ngois/logging": "*",
    "@ngois/service-kit": "*",
    "express": "^4.21.2",
    "http-proxy-middleware": "^3.0.5",
    "jose": "^6.0.11",
    "zod": "^3.25.28"
  },
  "devDependencies": {
    "@types/express": "^4.17.22",
    "tsx": "^4.19.4"
  }
}
