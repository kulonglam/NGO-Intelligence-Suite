{
  "name": "@ngois/auth-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@ngois/config": "*",
    "@ngois/db": "*",
    "@ngois/errors": "*",
    "@ngois/logging": "*",
    "@ngois/rbac": "*",
    "@ngois/service-kit": "*",
    "@ngois/tenant-context": "*",
    "jose": "^6.0.11",
    "zod": "^3.25.28"
  },
  "devDependencies": {
    "tsx": "^4.19.4"
  }
}
