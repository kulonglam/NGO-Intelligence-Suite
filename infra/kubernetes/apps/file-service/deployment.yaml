# STUB Deployment template values — replace image digests via CD.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: file-service
  namespace: ngois
  labels:
    app: file-service
    ngois.io/stub: "true"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: file-service
  template:
    metadata:
      labels:
        app: file-service
      annotations:
        # STUB: prometheus.io/scrape: "true"
        # STUB: prometheus.io/port: "3010"
    spec:
      serviceAccountName: file-service
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: file-service
          image: europe-west1-docker.pkg.dev/ngois-stub/ngois/file-service:digest-replace-me
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3010
              name: http
          env:
            - name: file-service
              value: file-service
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: ngois-db
                  key: url
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
---
apiVersion: v1
kind: Service
metadata:
  name: file-service
  namespace: ngois
spec:
  selector:
    app: file-service
  ports:
    - name: http
      port: 3010
      targetPort: http
