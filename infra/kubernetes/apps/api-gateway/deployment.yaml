# STUB Deployment template values — replace image digests via CD.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  namespace: ngois
  labels:
    app: api-gateway
    ngois.io/stub: "true"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-gateway
  template:
    metadata:
      labels:
        app: api-gateway
      annotations:
        # STUB: prometheus.io/scrape: "true"
        # STUB: prometheus.io/port: "3000"
    spec:
      serviceAccountName: api-gateway
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: api-gateway
          image: europe-west1-docker.pkg.dev/ngois-stub/ngois/api-gateway:digest-replace-me
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
              name: http
          env:
            - name: api-gateway
              value: api-gateway
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: ngois-db
                  key: url
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
---
apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: ngois
spec:
  selector:
    app: api-gateway
  ports:
    - name: http
      port: 3000
      targetPort: http
