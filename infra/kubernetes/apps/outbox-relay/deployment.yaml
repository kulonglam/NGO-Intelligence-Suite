# STUB Deployment template values — replace image digests via CD.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: outbox-relay
  namespace: ngois
  labels:
    app: outbox-relay
    ngois.io/stub: "true"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: outbox-relay
  template:
    metadata:
      labels:
        app: outbox-relay
      annotations:
        # STUB: prometheus.io/scrape: "true"
        # STUB: prometheus.io/port: "3020"
    spec:
      serviceAccountName: outbox-relay
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: outbox-relay
          image: europe-west1-docker.pkg.dev/ngois-stub/ngois/outbox-relay:digest-replace-me
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3020
              name: http
          env:
            - name: outbox-relay
              value: outbox-relay
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: ngois-db
                  key: url
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
---
apiVersion: v1
kind: Service
metadata:
  name: outbox-relay
  namespace: ngois
spec:
  selector:
    app: outbox-relay
  ports:
    - name: http
      port: 3020
      targetPort: http
